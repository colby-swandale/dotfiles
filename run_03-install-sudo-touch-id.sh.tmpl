#!/bin/bash

COMMAND_NAME="sudo-touchid"
BIN_PATH="$({{ .brew_path }} --prefix ${COMMAND_NAME})/bin/${COMMAND_NAME}"

if [[ -f $BIN_PATH ]]; then
  eval $BIN_PATH
else 
  echo "sudo-touchid not found"
fi